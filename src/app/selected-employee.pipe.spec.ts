import { SelectedEmployeePipe } from './selected-employee.pipe';

describe('SelectedEmployeePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedEmployeePipe();
    expect(pipe).toBeTruthy();
  });
});
